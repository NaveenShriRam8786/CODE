#include <iostream>
#include <algorithm>
using namespace std;

int main(){
    int a[] = {50, 20, 40, 10, 30};
    for(int i:a)
    {
        cout<<i<<endl;
    }
    cout<<"After Sorting"<<endl;
    sort(a, a + 5);
    for (int i : a)
    {
        cout<<i<<endl;
    }
    return 0;
}
